{
  "name": "nebras-open-finance-postman",
  "version": "1.0.0",
  "private": true,
  "description": "UAE Open Finance Postman collections + flow tests",
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "test:insurance": "node --test tests/insurance/*.test.js",
    "test:banking": "node --test tests/banking/*.test.js"
  },
  "devDependencies": {
    "newman": "^6.2.1"
  }
}
